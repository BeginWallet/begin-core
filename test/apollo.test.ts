// import { createMockClient } from '@apollo/client/testing'

describe('Test Apollo Client', () => {
    it('Mock Provider', () => {
        // const client = new ApolloClient({
        //     cache: new InMemoryCache()
        // });
        // client.
    });
});